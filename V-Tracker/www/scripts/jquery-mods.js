//Modifications to jQuery are done in here...

$(document).bind("mobileinit", function(){
	//set default transitions to "none", it looks cleaner
	$.mobile.defaultPageTransition = 'none';
	$.mobile.defaultDialogTransition = 'none';			 
});